name: Environment Variables
on:
  push:
jobs:

  env-vars-and-context:
    runs-on: ubuntu-latest
    steps:

      - name: log env var
        run: echo $GITHUB_REF

      - name: log context
        run: echo ${{ github.ref }}
